import { NavLink } from 'react-router-dom';

const Character = () => {
  return (
    <div>
      Character
      <NavLink to="/planets">planets</NavLink>
    </div>
  );
};

export default Character;
